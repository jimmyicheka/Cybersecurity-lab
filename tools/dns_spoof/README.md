## 📌 Requirements

To run this DNS spoofing script successfully, make sure the following dependencies and configurations are set up.

---

### 🖥️ System Requirements
- Linux-based OS (recommended: **Kali Linux**)
- **Root privileges** (required for NFQUEUE + iptables)

---

### 🐍 Python Requirements

Install required Python packages:

```bash
pip install scapy netfilterqueue
